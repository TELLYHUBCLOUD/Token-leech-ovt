# The user wants "remove the buttons and make it automatically download all qualities and send to user".
# That means if the user sends `/bypass <url>`, it shouldn't just be bypass results, it should trigger LEECH commands.
# Wait, should `/bypass` trigger `leech` for EVERYTHING, or just for these multi-quality links?
# The user's exact words: "Ek kam karo ye button bala sab hata do or asa karo ke ke ye apne ap sab quality download karna start kar dega or user ko send karna start kar dega"
# "Do one thing remove all these buttons and make it so that it automatically starts downloading all qualities and starts sending to user."
# This refers to the multi-quality links (rareanimes/codedew).
